type: object
properties:
  data:
    type: object
    properties:
      type:
        type: string
        example: accounts
      id:
        type: string
        description: Conformity account ID.
        example: H19NxMi5-
      attributes:
        type: object
        properties:
          settings:
            type: object
            properties:
              rules:
                type: array
                items:
                  type: object
                  properties:
                    riskLevel:
                      $ref: "../../../../../../shared-schemas/risk-level.yml"
                    id:
                      type: string
                      description: Rule ID.
                      example: RDS-018
                    extraSettings:
                      $ref: "../../../../../../shared-schemas/extra-settings.yml"
                    enabled:
                      type: boolean
                      description: false if the rule is manually configured to be disabled for the account.
                      example: false
                    provider:
                      $ref: "../../../../../../shared-schemas/provider.yml"
      relationships:
        type: object
        properties:
          organisation:
            $ref: "../../../../../../shared-schemas/organisation-short.yml"
  meta:
    type: object
    properties:
      notes:
        type: array
        description: List of notes for the rule.
        items:
          type: object
          properties:
            createdBy:
              type: string
              description: ID of the user who has created the note.
              example: "SYmS0YcL-"
            createdDate:
              type: integer
              description: A date-time in Unix Epoch timestamp format (in milliseconds) which
                is when the note has been created.
              example: 1511456432526
            note:
              type: string
              description: Note added to the rule.
              example: hello world
