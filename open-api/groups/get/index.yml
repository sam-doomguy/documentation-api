summary: List All Groups
description: This endpoint allows you to query all groups with accounts that you have access to.
tags:
  - Groups
responses:
  200:
    description: OK
    content:
      application/json:
        schema:
          $ref: ./responses/200.yml
  401:
    description: Unauthorized.
    content:
      application/json:
        schema:
          $ref: ../../shared-schemas/responses/401.yml
  403:
    description: Unauthorized. The requesting user does not have enough privilege.
    content:
      application/json:
        schema:
          $ref: ../../shared-schemas/responses/403.yml
security:
  - ApiKeyAuth: []
