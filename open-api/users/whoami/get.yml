summary: Get current user
description: Gets current user.
responses:
  200:
    description: "The request has succeeded"
    content:
      application/json:
        schema:
          $ref: "../schemas/get-whoami-200-response.yml"
  403:
    description: "Forbidden. This happens when a valid api key is not passed."
    content:
      application/json:
        schema:
          $ref: "../../shared-schemas/403.yml"
security:
  - Authorizer: []