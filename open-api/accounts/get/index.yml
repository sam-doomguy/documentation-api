summary: Get all accounts
description: This endpoint is used to get all of the Cloud Conformity accounts of the user's organisation.
tags:
  - Accounts
responses:
  200:
    description: OK
    content:
      application/json:
        schema:
          $ref: ./responses/200.yml
  401:
    description: Unauthorized. The requesting user does not have enough privilege.
    content:
      application/json:
        schema:
          $ref: ../../shared-schemas/responses/401.yml
security:
  - ApiKeyAuth: []
