summary: Get a user
description: Gets a user by id.
parameters:
  - in: path
    name: id
    schema:
      type: string
    required: true
    description: User Id to get
responses:
  200:
    description: "Forbidden. This happens when a valid api key is not provided."
    content:
      application/json:
        schema:
          $ref: "../../shared-schemas/403.yml"
  401:
    description: "Unauthorized. The requester user does not have enough privilege."
    content:
      application/json:
        schema:
          $ref: "../../shared-schemas/401.yml"
  403:
    description: "Forbidden. This happens when a valid api key is not provided."
    content:
      application/json:
        schema:
          $ref: "../../shared-schemas/403.yml"
  404:
    description: "Not Found. This happens when the provided id ."
    content:
      application/json:
        schema:
          $ref: "../../shared-schemas/403.yml"
security:
  - Authorizer: []