description: Bad Request. Cannot process request due to a server error.
content:
  application/json:
    schema:
      type: object
      properties:
        errors:
          type: array
          items:
            type: object
            properties:
              status:
                type: integer
                example: 500
                description: Error code.
              detail:
                type: string
                example: Internal Server Error
