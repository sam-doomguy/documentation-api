type: object
properties:
  type:
    type: string
    default: api-keys
  id:
    type: string
    description: Conformity ID of the account.
    example: BJ0Ox16Hb
  attributes:
    type: object
    properties:
      created-date:
        type: number
        description: A date-time in Unix Epoch timestamp format (in milliseconds)
          which is the date that the check was created.
        example: 1597041366544
      status:
        type: string
        description: The status of the api key.
        enum:
          - ENABLED
          - DISABLED
      last-used-date:
        type: number
        description: A date-time in Unix Epoch timestamp format (in milliseconds)
          which is the date that api key was last used.
        example: 1597041366544
      ips:
        type: array
        description: White listed IPs for this api keys.
        items:
          type: string
          example: "0.0.0.0/0"
