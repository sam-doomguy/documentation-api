summary: Get all users
description: Gets all of the users of the requester user's organisation.
responses:
  200:
    description: "200 response"
    content:
      application/json:
        schema:
          $ref: "./schemas/get-users-200-response.yml"
  401:
    description: "401 response"
    content:
      application/json:
        schema:
          $ref: "./schemas/get-users-401-response.yml"
security:
  - Authorizer: []